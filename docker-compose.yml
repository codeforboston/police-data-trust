version: "3"
services:
  db:
    image: postgres:13.2 #AWS RDS latest version
    restart: always
    env_file:
      - ".env"
    volumes:
      - ./init-user-db.sh:/docker-entrypoint-initdb.d/init-user-db.sh
      - postgres:/var/lib/postgresql/data
    ports:
      - 5432:5432
  web:
    restart: always
    build:
      context: ./frontend
    volumes:
      - ./frontend:/app
      # Prevents the host node_modules from clobbering the image's
      - /app/node_modules
    environment:
      NEXT_PUBLIC_API_MODE: real
      NEXT_PUBLIC_API_BASE_URL: http://localhost:5000/api/v1
    ports:
      - 3000:3000
  api:
    restart: always
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    command: bash -c '/wait && flask psql create && flask psql init && jupyter notebook --allow-root --ip=0.0.0.0'
    volumes:
      - .:/app
    depends_on:
      - db
    environment:
      POSTGRES_HOST: db
      FLASK_ENV: ${FLASK_ENV:-development}
      WAIT_HOSTS: db:5432
    ports:
      - 5000:5000
      - 8888:8888

volumes:
  postgres: {}
